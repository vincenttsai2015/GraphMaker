# GraphMaker 執行手冊

在晶創26（`nano4.nchc.org.tw`）上跑 GraphMaker，資料是注入 burst / hysteresis
巨觀動態的序列，依 global node id 併成單一靜態屬性圖。

帳號沿用 iService 的，`--account` 是 `acd109125`。

| 項目 | 值 |
|---|---|
| 登入 | `ssh <帳號>@nano4.nchc.org.tw` |
| partition | `8gpus`（48 小時，腳本標頭的預設） |
| GPU 宣告 | `--gres=gpu:1` |
| 每張 GPU 上限 | 12 CPU cores、200 GB 記憶體 |

資料在 repo 裡（4.7 MB），不用另外傳檔。

## 分工

一人負責一個 seed，互不干擾。以下把 `<S>` 換成自己的 seed 編號。

| seed | 負責人 | index |
|---|---|---|
| 0 | 奕諾 | 0~23 |
| 1 | 永結 | 24~47 |
| 2 | 嘉憲 | 48~71 |

queue 名額是各帳號獨立的 20 個，不用互相協調。

---

## A. 環境（只做一次）

沒有 conda module，要自己裝：

```bash
cd ~ && wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh && bash Miniconda3-latest-Linux-x86_64.sh -b -p ~/miniconda3 && ~/miniconda3/bin/conda init bash && source ~/.bashrc
```

```bash
conda create -n GraphMaker python=3.10 -y && conda activate GraphMaker
```

**不要照上游 README 的版本**（`torch 1.12.0+cu116`），那個在 H200 上跑不動。

```bash
PIP_USER=0 pip install torch==2.3.0 torchvision --index-url https://download.pytorch.org/whl/cu121
```

```bash
PIP_USER=0 pip install dgl -f https://data.dgl.ai/wheels/torch-2.3/cu121/repo.html
```

```bash
PIP_USER=0 pip install pandas scikit-learn pydantic wandb pyyaml huggingface_hub==0.30.2
```

### 驗證

**要在運算節點跑**，登入節點禁止計算：

```bash
srun --account=acd109125 --partition=dev --nodes=1 --gres=gpu:1 --cpus-per-task=12 --time=00:10:00 ~/miniconda3/envs/GraphMaker/bin/python -c "import torch,dgl; print(torch.__version__, dgl.__version__, torch.cuda.get_device_name(0), torch.cuda.get_device_capability(0))"
```

**要看到**：**NVIDIA H200** 與 **(9, 0)**。

### dgl.sparse

```bash
export LD_LIBRARY_PATH=$(~/miniconda3/envs/GraphMaker/bin/python -c "import nvidia,os,glob;print(':'.join(glob.glob(os.path.dirname(nvidia.__file__)+'/*/lib')))"):$LD_LIBRARY_PATH
```

```bash
srun --account=acd109125 --partition=dev --nodes=1 --gres=gpu:1 --cpus-per-task=12 --time=00:10:00 ~/miniconda3/envs/GraphMaker/bin/python -c "import dgl.sparse; print('dgl.sparse OK')"
```

**要看到**：`dgl.sparse OK`。送 job 不用自己 export，腳本裡已經有。

## B. 取得程式碼與資料

```bash
cd ~ && git clone --depth 1 https://github.com/vincenttsai2015/GraphMaker.git && cd GraphMaker && mkdir -p logs && chmod +x *.sh
```

### 驗證

```bash
cd ~/GraphMaker && ls data/macro/*.pkl | wc -l && ls -d configs/macro_* | wc -l && grep -c "GM_SEED" setup_utils.py && grep -h "num_epochs" configs/macro_wiki_vote_burst_support/train_Sync.yaml
```

**要看到**：依序是 **12**、**12**、**1**，最後一行的 `num_epochs : 100`。

```bash
cd ~/GraphMaker && bash run_macro_gm_nano4.sh --list | awk -v S=<S> '$4==S' | wc -l
```

**要看到**：**24**。清單本身有 120 個組合（含還沒分配的 seed 3、4），
你只會跑到自己那 24 個。

## C. 編譯 orca

```bash
cd ~/GraphMaker/orca && g++ -O2 -std=c++11 -o orca orca.cpp && ls -l orca && cd ~/GraphMaker
```

**要看到**：可執行檔 `orca`。`-Wreturn-type` 的 warning 可以忽略。

---

## D. 先跑一組

```bash
cd ~/GraphMaker && bash run_macro_gm_nano4.sh --list | awk -v S=<S> '$4==S' | head -3
```

**要看到**：三行，最後一欄都是 `<S>`。

```bash
cd ~/GraphMaker && sbatch --account=acd109125 --array=$((24*<S>)) run_macro_gm_nano4.sh
```

```bash
squeue --me
```

```bash
tail -40 ~/GraphMaker/logs/gm_macro_*_$((24*<S>)).out
```

**三件事都要對**：

1. `nvidia-smi` 那行是 **H200**
2. `Epoch N | best val X nll ... | best val E nll ... | patience N/20` 持續出現，
   nll 有往下
3. 訓練跑完 100 個 epoch 之後有 `===== SAMPLE =====`，
   以及 `Evaluator.summary()` 的輸出

## E. 全部送出

```bash
cd ~/GraphMaker && SEEDS="<S>" bash check_gm.sh
```

**要看到**：列出的 24 組 seed 欄都是 `<S>`。確認後送出：

```bash
SEEDS="<S>" bash check_gm.sh --submit
```

已完成的會自動跳過，所以跑到一半中斷、或有幾組失敗，重跑同一條就會補。

訓練成功但取樣失敗時，加 `GM_SKIP_TRAIN=1` 沿用已存的 checkpoint：

```bash
GM_SKIP_TRAIN=1 SEEDS="<S>" bash check_gm.sh --submit
```

那個變數要傳進 job，所以 `sbatch` 前面要加 `--export=ALL,GM_SKIP_TRAIN=1`——
`check_gm.sh` 會自動帶上。

## F. 看進度

```bash
cd ~/GraphMaker && SEEDS="<S>" bash check_gm.sh
```

也可以直接數結果檔：

```bash
ls ~/GraphMaker/results/*_seed<S>.log 2>/dev/null | wc -l
```

**要看到**：完成數，全部跑完是 **24**。

## G. 收集指標

```bash
cd ~/GraphMaker && grep -A20 "Evaluator" results/*_seed<S>.log | head -25
```

批次收集：

```bash
cd ~/GraphMaker && for f in results/*_seed<S>.log; do b=$(basename "$f" .log); m=$(grep -A20 "Evaluator" "$f" 2>/dev/null | tail -20 | tr '\n' ' '); [ -n "$m" ] && echo "${b}|${m}"; done > ~/gm_metrics_seed<S>.txt && wc -l < ~/gm_metrics_seed<S>.txt
```

**要看到**：**24** 行。

## H. 打包回傳

```bash
cd ~ && tar czf gm_seed<S>.tar.gz gm_metrics_seed<S>.txt GraphMaker/results/*_seed<S>.log && du -sh gm_seed<S>.tar.gz
```

從自己的電腦抓：

```bash
scp <帳號>@nano4.nchc.org.tw:~/gm_seed<S>.tar.gz .
```
