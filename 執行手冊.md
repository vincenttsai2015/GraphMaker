# GraphMaker 執行手冊

在晶創26（`nano4.nchc.org.tw`）上跑 GraphMaker。`--account` 是 `acd109125`。

| 項目 | 值 |
|---|---|
| 登入 | `ssh <帳號>@nano4.nchc.org.tw` |
| partition | `8gpus`（48 小時，腳本標頭的預設）／`dev`（4 小時） |
| GPU 宣告 | `--gres=gpu:1` |
| 每張 GPU 上限 | 12 CPU cores、200 GB 記憶體 |

## 分工

| seed | index |
|---|---|
| 0 | 0~63 |
| 1 | 64~127 |
| 2 | 128~191 |

以下把 `<S>` 換成自己的 seed 編號。queue 名額是各帳號獨立的 20 個。

config 與腳本都在 repo 裡，不要自己改——要調整的話改 repo 再讓大家 pull。

---

## A. 環境

沒有 conda module，要自己裝：

```bash
cd ~ && wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh && bash Miniconda3-latest-Linux-x86_64.sh -b -p ~/miniconda3 && ~/miniconda3/bin/conda init bash && source ~/.bashrc
```

```bash
conda create -n GraphMaker python=3.10 -y && conda activate GraphMaker
```

**不要照上游 README 的版本**（`torch 1.12.0+cu116`），那個在 H200 上跑不動。

```bash
PIP_USER=0 pip install torch==2.3.0 torchvision --index-url https://download.pytorch.org/whl/cu121
```

```bash
PIP_USER=0 pip install dgl -f https://data.dgl.ai/wheels/torch-2.3/cu121/repo.html
```

```bash
PIP_USER=0 pip install pandas scikit-learn pydantic wandb pyyaml huggingface_hub==0.30.2
```

### 驗證

要在運算節點跑，登入節點禁止計算：

```bash
srun --account=acd109125 --partition=dev --nodes=1 --gres=gpu:1 --cpus-per-task=12 --time=00:10:00 ~/miniconda3/envs/GraphMaker/bin/python -c "import torch,dgl; print(torch.__version__, dgl.__version__, torch.cuda.get_device_name(0), torch.cuda.get_device_capability(0))"
```

**要看到**：**NVIDIA H200** 與 **(9, 0)**。

### dgl.sparse

```bash
export LD_LIBRARY_PATH=$(~/miniconda3/envs/GraphMaker/bin/python -c "import nvidia,os,glob;print(':'.join(glob.glob(os.path.dirname(nvidia.__file__)+'/*/lib')))"):$LD_LIBRARY_PATH
```

```bash
srun --account=acd109125 --partition=dev --nodes=1 --gres=gpu:1 --cpus-per-task=12 --time=00:10:00 ~/miniconda3/envs/GraphMaker/bin/python -c "import dgl.sparse; print('dgl.sparse OK')"
```

**要看到**：`dgl.sparse OK`。送 job 不用自己 export，腳本裡已經有。

---

## B. 程式碼

```bash
cd ~ && git clone --depth 1 https://github.com/vincenttsai2015/GraphMaker.git && cd GraphMaker && mkdir -p logs && chmod +x *.sh
```

### 驗證

```bash
cd ~/GraphMaker && ls run_macro_gm.sh check_gm.sh macro_data.py gm_check.py && ls -d configs/macro_* | wc -l && grep -c "graphs_per_epoch" configs/macro_wiki_vote_burst_support/train_Sync.yaml && grep -c "Y=macro.test_Y" sample.py
```

**要看到**：四個檔案都在、config **32** 組、最後兩個數字不是 0。

```bash
cd ~/GraphMaker && bash run_macro_gm.sh --list | awk -v S=<S> '$4==S' | wc -l
```

**要看到**：**64**。清單本身有 320 個組合（含還沒分配的 seed 3、4）。

---

## C. 編譯 orca

```bash
cd ~/GraphMaker/orca && g++ -O2 -std=c++11 -o orca orca.cpp && ls -l orca && cd ~/GraphMaker
```

**要看到**：可執行檔 `orca`。`-Wreturn-type` 的 warning 可以忽略。

---

## D. 資料

安裝 rclone：

```bash
curl -O https://downloads.rclone.org/rclone-current-linux-amd64.zip && unzip -q rclone-current-linux-amd64.zip && mkdir -p ~/bin && cp rclone-*/rclone ~/bin/ && chmod +x ~/bin/rclone && echo 'export PATH="$HOME/bin:$PATH"' >> ~/.bashrc && export PATH="$HOME/bin:$PATH"
```

`rclone.conf` 放到 `~/.config/rclone/rclone.conf` 並 `chmod 600`。

```bash
rclone listremotes
```

**要看到**：`gdrive:`。

```bash
cd ~ && rclone copy --drive-root-folder-id 1wBCissrBmxsVubagKFIvzQoRR83snwLP gdrive:gm_data_cond.tar.gz . -P && mkdir -p GraphMaker/data/macro && tar xzf gm_data_cond.tar.gz -C GraphMaker/data/macro
```

### 驗證

```bash
cd ~/GraphMaker && ls data/macro/*.pkl | wc -l && ~/miniconda3/envs/GraphMaker/bin/python -c "
import json
m=json.load(open('data/macro/macro_wiki_vote_burst_support_meta.json',encoding='utf-8'))
print(f\"節點 {m['num_nodes']}  Y {m['num_classes_Y']} 類  觀測窗 0..{m['t_split']-1}  目標窗 {m['t_target']} 張\")
print(f\"訓練 {m['n_train_graphs']} 張 / 測試 {m['n_test_seq']} 條\")"
```

**要看到**：**32** 個 pkl、節點 **200**、Y **80** 類、目標窗 **16** 張。

```bash
cd ~/GraphMaker && ~/miniconda3/envs/GraphMaker/bin/python -c "
from macro_data import MacroCondDataset
import numpy as np
d = MacroCondDataset('macro_wiki_vote_burst_support')
n = len(d)//16
per_t = [np.mean([len(d.train[s*16+k]['src']) for s in range(n)]) for k in range(16)]
print('訓練圖每張平均邊數 t=16..31:', ' '.join(f'{v:.0f}' for v in per_t))
print('平均度 %.2f' % (2*np.mean([len(g['src']) for g in d.train])/d.num_nodes))"
```

**要看到**：邊數在中段隆起（burst 峰值落在目標窗）、平均度約 **1.7**。

---

## E. 先跑一組

```bash
cd ~/GraphMaker && bash run_macro_gm.sh --list | awk -v S=<S> '$4==S' | head -3
```

**要看到**：三行，最後一欄都是 `<S>`。

```bash
cd ~/GraphMaker && sbatch --account=acd109125 --partition=dev --time=04:00:00 --array=$((64*<S>)) run_macro_gm.sh
```

```bash
squeue --me
```

```bash
tail -40 ~/GraphMaker/logs/gm_macro_*_$((64*<S>)).out
```

**四件事都要對**：

1. `nvidia-smi` 那行是 **H200**
2. 開頭有 `訓練圖 6400 張，節點 200，Y 80 類`
3. `Epoch N | best val X nll ... | best val E nll ... | patience N/20` 持續出現，
   nll 有往下
4. `===== SAMPLE =====` 之後有 `測試序列 N 條 x 16 張`，最後印出每張平均邊數

### 驗證形狀

```bash
cd ~/GraphMaker && ~/miniconda3/envs/GraphMaker/bin/python -c "
import pickle,glob,numpy as np
p=glob.glob('$HOME/gm_generated/*wiki_vote*/GraphMaker/sampled_ts.pkl')[0]
g=pickle.load(open(p,'rb'))
e=[[x.number_of_edges() for x in s] for s in g]
print(f'{len(g)} 條 x {len(g[0])} 張，節點 {g[0][0].number_of_nodes()}')
print('每張平均邊數:', ' '.join(f'{v:.0f}' for v in np.mean(e,axis=0)))
print('相鄰兩張完全相同的比例: %.1f%%' % (100*np.mean(
    [set(s[i].edges())==set(s[i+1].edges()) for s in g for i in range(len(s)-1)])))"
```

**要看到**：每條 **16 張**、節點 **200**、相鄰相同比例接近 **0%**。
是 100% 表示條件沒生效，回 B 段驗證。

---

## F. 全部送出

```bash
cd ~/GraphMaker && SEEDS="<S>" bash check_gm.sh
```

**要看到**：列出的 64 組 seed 欄都是 `<S>`。確認後送出：

```bash
SEEDS="<S>" bash check_gm.sh --submit
```

已完成的會自動跳過，跑到一半中斷或有幾組失敗，重跑同一條就會補。

訓練成功但取樣失敗時，加 `GM_SKIP_TRAIN=1` 沿用已存的 checkpoint：

```bash
GM_SKIP_TRAIN=1 SEEDS="<S>" bash check_gm.sh --submit
```

---

## G. 看進度

```bash
cd ~/GraphMaker && SEEDS="<S>" bash check_gm.sh
```

```bash
ls ~/gm_generated/*_seed<S>/GraphMaker/sampled_ts.pkl 2>/dev/null | wc -l
```

**要看到**：全部跑完是 **64**。

---

## H. 打包回傳

生成的圖一定要帶回來，指標由 `eval_all_metrics.py` 事後統一算。

```bash
cd ~ && tar czf gm_seed<S>.tar.gz gm_generated/*_seed<S>/ GraphMaker/results/*_seed<S>.log && du -sh gm_seed<S>.tar.gz
```

```bash
cd ~ && rclone copy gm_seed<S>.tar.gz --drive-root-folder-id 1ep6RYcCxk7_AO5tmqZG-o6RWydOj9gKw gdrive: -P
```

上傳失敗顯示 `invalid_grant: maybe token expired` 的話，是 OAuth token 過期，
要在本機 `rclone config reconnect gdrive:` 之後把 `rclone.conf` 重新傳過來。
